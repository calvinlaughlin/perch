<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>perch</string>
	<key>CFBundleIconFile</key>
	<string>perch</string>
	<key>CFBundleIdentifier</key>
	<string>@BUNDLE_ID@</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>perch</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>@VERSION@</string>
	<key>CFBundleVersion</key>
	<string>@VERSION@</string>
	<key>LSMinimumSystemVersion</key>
	<string>14.0</string>

	<!-- Accessory app: no dock icon, no menu bar. Mirrors setActivationPolicy(.accessory) in
	     main.swift; setting it here too means perch is correct from the instant it launches
	     rather than flashing a dock icon first. -->
	<key>LSUIElement</key>
	<true/>

	<!-- Read-only, and only when `widget = calendar` is configured: perch draws what is left of
	     today in the notch. Nothing is written and nothing leaves the machine. "Full access" is
	     what macOS calls read access to events — the narrower write-only grant cannot read them.

	     This string is load-bearing. TCC terminates a process that touches EventKit without it,
	     so it ships with the first call rather than after it. -->
	<key>NSCalendarsFullAccessUsageDescription</key>
	<string>perch shows what is next on your calendar in the notch.</string>

	<key>NSHighResolutionCapable</key>
	<true/>
	<key>NSSupportsAutomaticTermination</key>
	<false/>
	<key>NSSupportsSuddenTermination</key>
	<false/>
</dict>
</plist>
